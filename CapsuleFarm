local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local player = Players.LocalPlayer
local playerGui = player.PlayerGui

-- Create UI elements
local screenGui = Instance.new("ScreenGui")
local titleLabel = Instance.new("TextLabel")
local timerLabel = Instance.new("TextLabel")
local collectingLabel = Instance.new("TextLabel")
local toggleButton = Instance.new("TextButton")
local warningLabel = Instance.new("TextLabel")

screenGui.Parent = playerGui
screenGui.ResetOnSpawn = false

-- Title label configurations
titleLabel.Size = UDim2.new(0, 400, 0, 50)
titleLabel.Position = UDim2.new(0.5, -200, 0.05, 0) -- Title Position
titleLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
titleLabel.TextColor3 = Color3.fromRGB(0, 0, 0)
titleLabel.TextScaled = true
titleLabel.Text = "Automatically Farms Capsule"
titleLabel.Parent = screenGui

-- Warning label configurations
warningLabel.Size = UDim2.new(0, 400, 0, 50)
warningLabel.Position = UDim2.new(0.5, -200, 0.15, 0) -- Positioned below the title
warningLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
warningLabel.TextColor3 = Color3.fromRGB(255, 0, 0) -- Red color for warnings
warningLabel.TextScaled = true
warningLabel.Text = "Will only work if you have 500 or more currency!"
warningLabel.Parent = screenGui

-- Timer label configurations
timerLabel.Size = UDim2.new(0, 200, 0, 50)
timerLabel.Position = UDim2.new(0.5, -100, 0.25, 0) -- Timer Position
timerLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
timerLabel.TextColor3 = Color3.fromRGB(0, 0, 0)
timerLabel.TextScaled = true
timerLabel.Text = "Timer: 00:30"
timerLabel.Parent = screenGui

-- Button configurations
toggleButton.Size = UDim2.new(0, 200, 0, 50)
toggleButton.Position = UDim2.new(0.5, -100, 0.35, 0) -- Moved above collecting label
toggleButton.BackgroundColor3 = Color3.fromRGB(0, 255, 0) -- Green color for active
toggleButton.TextColor3 = Color3.fromRGB(0, 0, 0)
toggleButton.TextScaled = true
toggleButton.Text = "Start Timer"
toggleButton.Parent = screenGui

-- Collecting label configurations
collectingLabel.Size = UDim2.new(0, 200, 0, 50)
collectingLabel.Position = UDim2.new(0.5, -100, 0.45, 0) -- Moved below the toggle button
collectingLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
collectingLabel.TextColor3 = Color3.fromRGB(0, 0, 0)
collectingLabel.TextScaled = true
collectingLabel.Text = "Collecting"
collectingLabel.Visible = false -- Start as not visible
collectingLabel.Parent = screenGui

-- Variables to control timer
local totalTime = 30 -- Duration for testing in seconds
local timerRunning = false
local elapsedTime = 0
local timerThread
local dehashedOnce = false -- Flag to ensure dehashing only happens once

-- Dehashing and remote inspection logic
local function initializeRemotes()
    local Fsys = require(game.ReplicatedStorage:WaitForChild("Fsys")).load

    -- Get the init function from RouterClient
    local initFunction = Fsys("RouterClient").init

    -- Folder containing the remotes to track
    local remoteFolder = game.ReplicatedStorage:WaitForChild("API")

    -- A flag to ensure we print only once during the initial scan
    
    -- Function to inspect upvalues and identify remotes
    local function inspectUpvalues()
        local remotes = {}  -- Table to collect remotes

        for i = 1, math.huge do
            local success, upvalue = pcall(getupvalue, initFunction, i)
            if not success then
                break
            end
            
            -- If the upvalue is a table, let's check its contents
            if typeof(upvalue) == "table" then
                for k, v in pairs(upvalue) do
                    -- Check for RemoteEvents, RemoteFunctions, BindableEvents, and BindableFunctions
                    if typeof(v) == "Instance" then
                        if v:IsA("RemoteEvent") or v:IsA("RemoteFunction") or v:IsA("BindableEvent") or v:IsA("BindableFunction") then
                            -- Log the key, type of value, and value
                            table.insert(remotes, {key = k, remote = v})
                            -- If it's the first time scanning, print remote information
                            if not printedOnce then
                                print("Key: " .. k .. " Type: " .. typeof(k) .. ", Value Type: " .. typeof(v))
                                print("Found remote: " .. v:GetFullName())
                            end
                        end
                    end
                end
            end
        end

        return remotes
    end

    -- Function to rename remotes based on their key
    local function rename(remote, key)
        local nameParts = string.split(key, "/")  -- Split the key by "/"
        if #nameParts > 1 then
            local remotename = table.concat(nameParts, "/", 1, 2)  -- Join the first two parts
            remote.Name = remotename
        else
            warn("Invalid key format for remote: " .. key)  -- Notify if the key format is incorrect
        end
    end

    -- Function to rename all existing remotes in the folder
    local function renameExistingRemotes()
        local remotes = inspectUpvalues()

        -- Rename all collected remotes based on the key
        for _, entry in ipairs(remotes) do
            rename(entry.remote, entry.key)
        end
    end

    -- Function to display dehashed message
    local function displayDehashedMessage()
        local uiElement = game:GetService("Players").LocalPlayer.PlayerGui.HintApp.LargeTextLabel
        uiElement.Text = "Remotes have been Dehashed!"
        uiElement.TextColor3 = Color3.fromRGB(0, 255, 0)  -- Set text color to green
        wait(3)
        uiElement.Text = ""
        uiElement.TextColor3 = Color3.fromRGB(255, 255, 255)  -- Reset text color to default (white)
    end

    -- Monitor for new remotes added to the folder
    local function monitorForNewRemotes()
        remoteFolder.ChildAdded:Connect(function(child)
            if child:IsA("RemoteEvent") or child:IsA("RemoteFunction") or child:IsA("BindableEvent") or child:IsA("BindableFunction") then
                print("New remote added: " .. child:GetFullName())
                -- Check and rename the new remote
                local remotes = inspectUpvalues()
                for _, entry in ipairs(remotes) do
                    rename(entry.remote, entry.key)
                end
            end
        end)
    end

    -- Start the periodic check in a coroutine (non-blocking)
    coroutine.wrap(monitorForNewRemotes)()

    -- Initial scan and rename for all existing remotes (print once)
    renameExistingRemotes()

    -- Display dehashed message
    displayDehashedMessage()

    -- Set the flag to prevent repeated dehashing
    dehashedOnce = true
end

local function startTimer()
    if not timerRunning then
        if not dehashedOnce then
            initializeRemotes() -- Run the dehashing function once
        end

        timerRunning = true
        elapsedTime = 0 -- Reset the elapsed time
        collectingLabel.Visible = false -- Hide Collecting message
        toggleButton.Text = "Stop Timer"
        timerLabel.Text = "Timer: 00:30" -- Reset timer display

        -- Timer loop
        timerThread = coroutine.create(function()
            while timerRunning do
                wait(1) -- Wait for 1 second
                elapsedTime += 1 -- Increment elapsed time by 1 second
                local remainingTime = totalTime - elapsedTime
                
                -- Calculate minutes and seconds
                local minutes = math.floor(remainingTime / 60)
                local seconds = remainingTime % 60

                -- Update the timer display
                timerLabel.Text = string.format("Timer: %02d:%02d", minutes, seconds)

                if remainingTime <= 0 then
                    -- After the timer reaches zero
                    collectingLabel.Visible = true

                    -- Check currency and invoke purchasing
                    local currencyText = playerGui.BucksIndicatorApp.CurrencyIndicator.Container.Amount.Text

                    -- Remove dollar sign and commas to parse currency
                    currencyText = currencyText:gsub("%$", ""):gsub(",", ""):gsub("%s+", "")
                    local currencyAmount = tonumber(currencyText)

                    if currencyAmount and currencyAmount >= 500 then
                        local args = {
                            [1] = "spring_2025_gacha_machine"
                        }
                        ReplicatedStorage:WaitForChild("API"):WaitForChild("LootBoxAPI/ExchangeItemForReward"):InvokeServer(unpack(args))
                    else
                        collectingLabel.Text = "Not enough currency!"
                    end
                    wait(1) -- Wait before resetting
                    elapsedTime = 0 -- Reset elapsed time
                end
            end
        end)
        coroutine.resume(timerThread)
    else
        -- Stop the timer
        timerRunning = false
        collectingLabel.Visible = false
        toggleButton.Text = "Start Timer"
    end
end

toggleButton.MouseButton1Click:Connect(startTimer)
